{
    "name": "surucu-kursu-app",
    "version": "1.0.0",
    "scripts": {
        "install:all": "cd frontend && npm install && cd ../backend && npm install",
        "build": "cd frontend && npm run build",
        "start": "cd backend && npm run start",
        "dev": "npm run build && npm run start"
    }
}